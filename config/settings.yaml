# Human-editable settings — tweak these without touching code.
# This file gets filled in during onboarding (target roles come from you directly).

target_roles: []
# Example, once filled in:
# target_roles:
#   - name: CIO
#     priority_weight: 10
#   - name: SVP
#     priority_weight: 9

industries: []
# Example:
# industries:
#   - Lifesciences/Pharma

search_cadence:
  scheduled_runs_per_day: 3
  # Self-adjusting cadence (per PRD §8) is a later refinement — fixed cadence for v0.
