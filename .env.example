# Copy this file to ".env" and fill in real values there. ".env" itself is never
# committed to version history (see .gitignore) — this file is just a template.

ANTHROPIC_API_KEY=
USAJOBS_API_KEY=
USAJOBS_USER_AGENT_EMAIL=
